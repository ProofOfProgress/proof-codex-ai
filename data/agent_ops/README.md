# Agent ops mission logs

Append-only JSONL files — one per pipeline run.

- **Not committed to git** (local visibility only)
- Written by CEO + employees via `python3 -m shorts_bot.agent_ops`
- Watch with CLI (`agent_ops tail`) or web UI (`/agent-ops`)

See `docs/FOR_OWNER_AGENT_TEAM.md`.
