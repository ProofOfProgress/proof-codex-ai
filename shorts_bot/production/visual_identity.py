"""Peripheral visual identity — face, eye, and mask visibility (owner direction)."""

from __future__ import annotations


def face_eye_visibility_rules() -> str:
    """Inject into I2V, stills, scripts — NOT silhouettes-only until finale."""
    return (
        "FACE & EYE RULE (Peripheral): "
        "Macro staring EYE is brand hero — use in hook and mid-beats, not saved for finale only. "
        "Metal/beak/numbered MASKS face camera directly anytime; the mask IS the face. "
        "Analog beats (hallway, mirror, CCTV): partial faces in shadow OK — vary POV, silhouette, and visible eyes. "
        "Final scare beat AMPS intensity (lunge, iris fill, mask close-up) — not the first reveal of eyes/masks. "
        "No real-person celebrity likeness; no gore."
    )


def face_eye_visibility_compact() -> str:
    return (
        "Eyes and masks visible when the story needs them — hook can open on macro eye or mask; "
        "finale escalates, not debut-only."
    )
