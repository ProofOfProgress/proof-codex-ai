# TikTok Shop Factory

API connections + clip pipeline. **Strategy lives in the owner's paid course** (paste transcripts to `data/research/course/`).

```bash
bash scripts/install.sh
python3 -m shorts_bot.tiktok_shop status
python3 -m shorts_bot.web
python3 -m pytest tests/ -q
```

Basics: `docs/FOR_OWNER_BASICS.md`  
Secrets: `docs/CURSOR_SECRETS.md`
