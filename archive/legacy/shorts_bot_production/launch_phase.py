"""Retired launch phase — silent launch rules stub."""

from __future__ import annotations


def would_be_silent_launch(draft_id: int) -> bool:
    return False


def is_silent_launch_draft(draft_id: int) -> bool:
    return False


def silent_launch_script_rules() -> str:
    return ""
