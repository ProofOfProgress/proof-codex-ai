# Cursor plugins (proof-codex-ai)

Plugins vendored here load into Cursor Desktop via `scripts/higgsfield-setup.sh`.

| Plugin | Path | Use |
|--------|------|-----|
| **Higgsfield** | `higgsfield/` | Module 4 images + Module 5 video in chat (`generate_image`, `generate_video`) |

MCP server URLs also live in `.cursor/mcp.json.example` (copy to `.cursor/mcp.json` if needed).
