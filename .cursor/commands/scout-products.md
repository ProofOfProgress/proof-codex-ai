Run EchoTik product scout for TikTok Shop (Module 3).

1. Invoke `/echotik-researcher` or run scout CLI yourself.
2. `python3 -m shorts_bot.tiktok_shop.scout_cli run --preset middle_core --limit 10`
3. Summarize top 3 products in plain English for Isaac — score, why they pass filters, next command.

Do not invent products. EchoTik secrets must be on the VM (new agent run after adding).
