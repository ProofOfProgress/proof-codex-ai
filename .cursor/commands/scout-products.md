Run EchoTik product scout for TikTok Shop (Module 3).

Use `/affiliate-ceo` to orchestrate — or run scout CLI directly:

```bash
python3 -m shorts_bot.tiktok_shop.scout_cli run --preset middle_core --limit 10
python3 -m shorts_bot.tiktok_shop.scout_cli list
```

Summarize top 3 products in plain English. See `docs/FOR_OWNER_AGENT_TEAM.md`.
