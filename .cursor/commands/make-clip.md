Make one affiliate clip for TikTok Shop.

Use `/affiliate-ceo` — it delegates to `video-editor`, `video-caption-writer`, `module1-qc-runner`, etc.

See `docs/FOR_OWNER_AGENT_TEAM.md` and `python3 -m shorts_bot.agent_ops status` for the mission log.
